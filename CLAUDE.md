# 週末 2Dミニゲームプロジェクト AI開発ガイドライン

## 1. プロジェクトの基本方針（最重要）
このプロジェクトは「週末の限られた時間で2人で開発する2Dミニゲーム集」です。
以下の哲学を常に優先して提案やコード生成を行ってください。

- **小さく作る (Keep It Simple):** 過剰な設計（オーバーエンジニアリング）や将来のための複雑な拡張性は不要です。常に最もシンプルで直接的な解決策を提案してください。
- **Playable Build優先:** 「とりあえず遊べる状態」にすることを最優先します。未完成の複雑な機能よりも、動く最小限の機能を提示してください。
- **スコープ制御:** 機能要件を勝手に膨らませないでください。ユーザーが提示した要件を満たす最小のコードを提供してください。

## 2. 技術スタック & アーキテクチャ
- **エンジン / 言語:** Unity / C#
- **プラットフォーム:** Android / iPhone
- **アーキテクチャの境界:** プロジェクトは「共通基盤」と「各ミニゲーム固有」の処理に完全に分離されています。
  - `Assets/_Project/Common/`: 全ミニゲームで使い回す処理（Scene, Input, UI, Audio管理など）。ここには特定のミニゲームに依存するコードを書かないこと。
  - ミニゲーム固有のディレクトリ: 該当するミニゲームでのみ使用する処理。

## 3. Unity & C# 実装ガイドライン
- **Scene/Prefabの競合回避:** 2人でのGit開発を前提とするため、コンフリクトが起きやすい巨大なSceneやPrefabを作らない設計を心がけてください（例: 機能を小さなPrefabに分割する、ScriptableObjectでデータを切り出すなど）。
- **コンポーネント指向:** 1つのMonoBehaviourに全てを書くのではなく、役割（入力、移動、描画、状態管理）ごとに小さく分割したコンポーネントを提案してください。
- **コードスタイル:** 
  - メソッドは短く、単一の責任を持たせること。
  - マジックナンバーは避け、`[SerializeField]` や定数として定義すること。
  - コメントは「何をしているか（What）」よりも「なぜそうしているか（Why）」を中心に記述すること。

## 4. AIへの指示（回答のスタンス）
- 質問に対する回答は、挨拶や冗長な説明を省き、すぐに要点に入ってください。
- 実装を提案する際は、「ステップバイステップの具体的な手順」と「そのままコピーできる最小限のC#コード」をセットで提供してください。
- 複数のアプローチがある場合は、「この週末で実装し切るのに最も確実で簡単な方法」を第一候補として推奨してください。